import {makeProject} from '@motion-canvas/core';
import titleCard from './templates/title-card?scene';

export default makeProject({
  name: 'OpenTake Motion Canvas Studio',
  scenes: [titleCard],
});
