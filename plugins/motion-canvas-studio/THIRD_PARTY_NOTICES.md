# Third-party notices

This wrapper embeds Motion Canvas 3.17.2 (`@motion-canvas/core`,
`@motion-canvas/2d`, and the build-time `@motion-canvas/vite-plugin`). Motion
Canvas is Copyright © 2022 motion-canvas and distributed under the MIT License;
the complete license text is preserved in `LICENSE`.

Upstream identity: `motion-canvas/motion-canvas`, tag `v3.17.2`.
OpenTake modifications are limited to the deterministic offline runner, typed
template configuration, and the repository sample template.
